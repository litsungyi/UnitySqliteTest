using UnityEngine;
using System.Collections;

public class Main : MonoBehaviour {

	// Use this for initialization
	void Start () {
        var db = new DataService( "TestDb.db" );
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
